// Shared pixel constants so the frozen left column, sticky header, and every
// row/block agree on layout without recomputing it in three places.

export const LEFT_COL_PX = 236;
export const ROW_HEIGHT_PX = 60;
export const MONTH_ROW_PX = 30;
export const QUARTER_ROW_PX = 22;
